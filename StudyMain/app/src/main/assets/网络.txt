
1，TCP的3次握手和四次挥手
为什么TCP客户端最后还要发送一次确认呢？
    主要防止已经失效的连接请求报文突然又传送到了服务器，从而产生错误
    如果是 2次握手，客户端的消息延时到服务端，那么有可能延迟造成2次连接


2，TCP与UDP的区别与应用



3，HTTP协议



4，HTTP与HTTPS的区别以及如何实现安全性
    HTTPS：是以安全为目标的HTTP通道，简单讲是HTTP的安全版，即HTTP下加入SSL层，HTTPS的安全基础是SSL，因此加密的详细内容就需要SSL。




5，如何验证证书的合法性?
    1.可信性验证/合法性验证：是否是权威机构发布
　　2.完整性验证：在验证可信性的过程中，h1和h2的比较，其实已经顺便验证其完整性了。
　　3.有效性验证：比如验证它的Vilidity值，看是否过期...



4，https中哪里用了对称加密，哪里用了非对称加密，对加密算法（如RSA）等是否有了解?



5，客户端在使用HTTPS方式与Web服务器通信时有以下几个步骤，

　　（1）客户使用https的URL访问Web服务器，要求与Web服务器建立SSL连接。
　　（2）Web服务器收到客户端请求后，会将网站的证书信息（证书中包含公钥）传送一份给客户端。
　　（3）客户端的浏览器与Web服务器开始协商SSL连接的安全等级，也就是信息加密的等级。
　　（4）客户端的浏览器根据双方同意的安全等级，建立会话密钥，然后利用网站的公钥将会话密钥加密，并传送给网站。
　　（5）Web服务器利用自己的私钥解密出会话密钥。
　　（6）Web服务器利用会话密钥加密与客户端之间的通信


6 ， 各种http 返回的 状态码

    状态码	响应类别	原因短语
    1XX	信息性状态码（Informational）	服务器正在处理请求
    2XX	成功状态码（Success）	请求已正常处理完毕
    3XX	重定向状态码（Redirection）	需要进行额外操作以完成请求
    4XX	客户端错误状态码（Client Error）	客户端原因导致服务器无法处理请求
    5XX	服务器错误状态码（Server Error）	服务器原因导致处理请求出错


200状态码：表示请求已成功，请求所希望的响应头或数据体将随此响应返回
202状态码：服务器已接受请求，但尚未处理

301状态码：被请求的资源已永久移动到新位置。服务器返回此响应（对 GET 或 HEAD 请求的响应）时，会自动将请求者转到新位置。
302状态码：请求的资源临时从不同的URI响应请求，但请求者应继续使用原有位置来进行以后的请求



403 Forbidden
表示对请求资源的访问被服务器拒绝了
404 Not Found
表示服务器找不到你请求的资源
500 Internal Server Error
表示服务器执行请求的时候出错了
503 Service Unavailable
表示服务器超负载或正停机维护，无法处理请求



retrifit 源码分析
https://juejin.im/entry/59ad567df265da24777a161d



http劫持怎么办？
1，dns劫持（ip）
2，消息内容劫持（ 内容校验，加密  ）

http弱网情况下怎么处理：
    开启http的gzip压缩


java（Android）平台自带http的api：HttpURLConnection ：

    Apache 提供的api：
    HttpClient， android5.0被废弃，6.0逐渐删除。

Square 的  OkHttp：
    高效， 支持 GZip 压缩数据

Volley：
    对大数据量的网络请求的表现并不好



Http 1.0与1.1的区别
最主要的区别就是连接时，在1.0中只能1个单独的连接只能处理1次请求，1.1中可以执行多个请求，
并且多个请求可以叠加而不用等待一个请求结束再发送下一个请求。



由于XMPP的缺点：流量大（基于XML），不可靠（为传统固定网络设计，没有考虑WIFI/2G/3G/地铁/电梯等复杂网络场景），交互复杂（登陆需5-6次，尤其是TLS握手）；XMPP丢消息的根本原因：服务端和客户端处于“半关闭”状态，客户端假连接状态，服务端有收不到回执；Server端连接层和逻辑层代码没有解耦分离，常常重启导致不可用；


一个IM方案要求：
心跳方案：
    GCM策略，心跳时间，消息轮训
通信协议
数据库设计方式



设计一个数据下载模块（断点续传）

HttpClient、HttpURLConnection、OKHttp和Volley优缺点和性能对比,如何选择？
https://blog.csdn.net/langtop/article/details/77972855




